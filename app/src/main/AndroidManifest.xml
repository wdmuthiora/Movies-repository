<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moringaschool.android_ip_1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_Ip_1">
        <activity
            android:name=".UI.FavouriteMoviesActivity"
            android:exported="false" />
        <activity
            android:name=".UI.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.SignUpActivity" />
        <activity android:name=".UI.LoginActivity" android:theme="@style/Theme.AppCompat.NoActionBar"> </activity>
        <activity android:name=".UI.MovieCastActivity" />
        <activity android:name=".UI.DetailsActivity" />
        <activity android:name=".UI.MovieSearchActivity" />
    </application>

</manifest>